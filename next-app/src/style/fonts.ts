import { Inter, Kodchasan } from "next/font/google";

const inter = Inter({ subsets: ["latin"], display: "swap" });
const kodchasan = Kodchasan({ subsets: ["latin"], weight: "700", display: "swap" });

export { inter, kodchasan };
